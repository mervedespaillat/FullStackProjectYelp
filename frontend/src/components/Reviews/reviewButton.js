import { useSelector } from "react-redux"

const ReviewButton = ({shop,id}) => {
    const sessionUser = useSelector((state) => state.session.user)


    // return sessionUser ? (
    //     <link to={`/reviews/`}
    // )

}